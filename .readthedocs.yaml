version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.8"

build:
   apt_packages:
      - libgdal20
      
sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements-dev.txt