version: 2

build:
  os: "ubuntu-20.04"
  apt_packages:
    - libgdal-dev
    - libpq-dev
    - gdal-bin
    - libpq5=12.2-4
    - libgdal-dev

  tools:
    python: "3.9"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements-dev.txt